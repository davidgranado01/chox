
-- ############################################################################################
-- START VERSION V2.4.0
-- ############################################################################################\


-- ONLINE SUPPORT FORM

CREATE TABLE support_message
(
  id serial NOT NULL,
  claim_id integer,
  subject character varying(100),
  created_by integer,
  last_modified_by integer,
  supplier_reference character varying(20),
  last_modified_date timestamp without time zone,
  created_date timestamp without time zone,
  message text,
  CONSTRAINT support_message_pkey PRIMARY KEY (id)
)
WITH (OIDS=FALSE);
ALTER TABLE support_message OWNER TO postgres;

------------------------------------------------------------------------------------------------

ALTER TABLE support_message ALTER COLUMN id SET DEFAULT nextval('support_message_id_seq'::regclass);

------------------------------------------------------------------------------------------------