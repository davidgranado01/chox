---------------------------------------------------------------------------
-- 6.15.2 Automated discount off Hire Costs (extension on to-do 6.1.1)
---------------------------------------------------------------------------

ALTER TABLE insurer_discount ADD COLUMN is_applied_to_penalties BOOLEAN DEFAULT FALSE;

ALTER TABLE insurer_discount ADD COLUMN discount_type INT;

UPDATE insurer_discount SET discount_type = 2 WHERE discount_type IS NULL;

ALTER TABLE insurer_discount ALTER COLUMN discount_type SET NOT NULL;

ALTER TABLE invoice ADD COLUMN total_gross_insurer_discount numeric(10,2) NOT NULL DEFAULT 0.00;
ALTER TABLE invoice ADD COLUMN repair_gross_insurer_discount numeric(10,2) NOT NULL DEFAULT 0.00;
ALTER TABLE invoice ADD COLUMN hire_gross_insurer_discount numeric(10,2) NOT NULL DEFAULT 0.00;

ALTER TABLE invoice_original ADD COLUMN total_gross_insurer_discount numeric(10,2) NOT NULL DEFAULT 0.00;
ALTER TABLE invoice_original ADD COLUMN repair_gross_insurer_discount numeric(10,2) NOT NULL DEFAULT 0.00;
ALTER TABLE invoice_original ADD COLUMN hire_gross_insurer_discount numeric(10,2) NOT NULL DEFAULT 0.00;